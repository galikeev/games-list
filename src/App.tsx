import { Container } from '@mui/material';
import { GamesList } from './components';

function App() {
  return (
    <Container maxWidth="lg">
      <GamesList />
    </Container>
  );
}

export default App;
